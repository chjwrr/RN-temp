import { SCREEN_HEIGHT, SCREEN_WIDTH } from '@/utils';
import {
    StyleSheet,
  } from 'react-native';
  
export const styles = StyleSheet.create({
    bgImage:{
        flex:1,
    },
    test:{
       flex:1,
       width:300,
       height:500,
    }
});
  